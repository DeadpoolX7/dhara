{
  "name": "dhara",
  "version": "0.1.0",
  "description": "CLI tool for seamless file transfer between desktop and mobile devices",
  "main": "./dist/cli.js",
  "type": "module",
  "scripts": {
    "start": "bun run bin/cli.ts",
    "build": "bun build ./bin/cli.ts --compile --outfile=dist/dhara"
  },
  "bin": {"dhara": "./bin/cli.ts"},
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.2.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "cli-progress": "^3.12.0",
    "commander": "^14.0.0",
    "qrcode-terminal": "^0.12.0"
  }
}
